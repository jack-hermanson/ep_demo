import { FunctionComponent } from "react";
import { PageTitle } from "../../components/Layout/PageTitle";

export const IndexPage = () => {
    return (
        <div>
            <PageTitle title="EP Store Homepage" />
        </div>
    );
};
